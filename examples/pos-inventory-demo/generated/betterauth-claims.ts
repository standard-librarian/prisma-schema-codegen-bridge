// AUTO-GENERATED by @mdht/plugin-betterauth-claims. Do not edit by hand.
import type { DBFieldAttribute } from '@better-auth/core/db';
import { Schema } from 'effect';

export const RoleValues = ['CASHIER', 'MANAGER'] as const;
export type Role = (typeof RoleValues)[number];
const RoleStandardSchema = Schema.standardSchemaV1(Schema.Literal(...RoleValues));

export const roleField = {
  type: [...RoleValues],
  input: false, // set server-side only -- never trust a self-reported role from user input
  validator: { input: RoleStandardSchema, output: RoleStandardSchema },
} satisfies DBFieldAttribute;

/**
 * Spread into betterAuth({ user: { additionalFields } }) -- see
 * ../../../PLAN.md M4 for why this and the ZModel `@@allow`/`auth()`
 * side both trace back to the same `StaffMember` model's enum fields.
 */
export const additionalFields = {
  role: roleField,
} satisfies Record<string, DBFieldAttribute>;
