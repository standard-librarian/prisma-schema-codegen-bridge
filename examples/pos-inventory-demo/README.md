# examples/pos-inventory-demo

Minimal `Order` / `InventoryItem` / `Role` schema exercising all three
generator plugins, plus a compile-time check that the generated Jazz schema,
ts-rest contract, and claims types describe the same shape. See root
`PLAN.md` M5. Not yet implemented.
