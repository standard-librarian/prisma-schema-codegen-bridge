# packages/generator-core

Shared helpers for walking the ZModel AST / Prisma DMMF into a neutral
intermediate representation (model, fields, relations, attributes), used by
all three plugin packages so none of them re-implements AST traversal. See
root `PLAN.md` M1. Not yet implemented.
