# packages/plugin-ts-rest-contract

ZenStack plugin: IR -> a ts-rest contract with Effect Schema (Standard
Schema) validators, one per model marked `@@generate.contract`. See root
`PLAN.md` M3. Not yet implemented.
