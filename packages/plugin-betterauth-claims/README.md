# packages/plugin-betterauth-claims

ZenStack plugin: IR -> a `Role`/claims union type consumed both by
`@@allow` policy expressions and by BetterAuth's session-claims typing. See
root `PLAN.md` M4. Not yet implemented.
