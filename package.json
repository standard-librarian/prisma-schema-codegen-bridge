{
  "name": "prisma-schema-codegen-bridge",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "verify": "pnpm --filter pos-inventory-demo run verify",
    "pack:dry-run": "for pkg in generator-core plugin-jazz-schema plugin-ts-rest-contract plugin-betterauth-claims cli; do (cd packages/$pkg && npm pack --dry-run); done"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
